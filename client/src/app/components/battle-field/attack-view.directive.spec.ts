import { AttackViewDirective } from './attack-view.directive';

describe('AttackViewDirective', () => {
  it('should create an instance', () => {
    const directive = new AttackViewDirective();
    expect(directive).toBeTruthy();
  });
});
